import React from 'react';

const QuizInputBar = () => {
    return (
        <div className='quiz-input-bar'>
            <input type='range' />
        </div>
    );
};

export default QuizInputBar;
